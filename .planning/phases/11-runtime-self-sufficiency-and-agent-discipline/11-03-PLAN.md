---
phase: 11-runtime-self-sufficiency-and-agent-discipline
plan: "03"
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - README.md
  - docs/releasing.md
  - tests/smoke/cli.test.ts
  - .planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-UAT.md
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "The feedback scenario is represented in verification artifacts."
    - "Maintainers can verify the runtime is ready after install without manual repair."
    - "Users and maintainers understand the expected approval and fallback behavior."
  artifacts:
    - path: README.md
      provides: user-facing expectations for ready-to-run runtime behavior
      min_lines: 35
    - path: .planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-UAT.md
      provides: explicit verification of the feedback scenario
      min_lines: 20
    - path: tests/smoke/cli.test.ts
      provides: end-to-end coverage for the feedback-driven runtime and guidance fixes
      min_lines: 100
  key_links:
    - from: README.md
      to: src/services/assistants/install.ts
      via: documented runtime-ready install behavior
      pattern: ~/.copilot
---

<objective>
Verify the feedback scenario and document the new runtime-ready support model.

Purpose: The feedback should be closed out with explicit verification, not just code changes.
Output: Updated docs plus a UAT script that mirrors the observed target-system failures and expected post-fix behavior.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-CONTEXT.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-RESEARCH.md
@/Users/ajitg/Downloads/feedback.log
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the runtime-ready install expectations</name>
  <files>README.md, docs/releasing.md</files>
  <action>Update documentation so it clearly states that the bundled `~/.copilot` runtime should be ready immediately after install, that manual `npm install` inside the runtime is not part of the supported flow, and that Forge is the expected processor for GitHub Discussions analysis requests.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The docs clearly describe the runtime-ready support model and expected approval behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add UAT for the feedback scenario</name>
  <files>.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-UAT.md, tests/smoke/cli.test.ts</files>
  <action>Add a UAT script and any needed smoke assertions that mirror the feedback log: initial install, discussion-analysis request, no follow-on dependency repair, and no raw GraphQL fallback when Forge is available. Keep the verification environment-controlled and reproducible.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The feedback scenario is captured in repeatable verification artifacts.</done>
</task>

</tasks>

<verification>
- [ ] `README.md` explains the runtime-ready install expectation
- [ ] Phase 11 has a UAT script mirroring the feedback scenario
- [ ] `npm test` verifies the feedback-driven runtime and guidance fixes
- [ ] Support expectations for approval and fallback behavior are documented
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The observed feedback scenario is directly addressed by code and verification
- Maintainers have a concrete support story for these runtime and agent-discipline issues
  </success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-03-SUMMARY.md`
</output>
