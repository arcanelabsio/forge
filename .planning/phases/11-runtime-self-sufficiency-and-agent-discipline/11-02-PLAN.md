---
phase: 11-runtime-self-sufficiency-and-agent-discipline
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/services/assistants/summonables.ts
  - src/services/assistants/copilot.ts
  - tests/smoke/cli.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "The installed Copilot agent tells the assistant to let Forge process the request."
    - "The installed agent requests approval once, then treats the Forge-managed workflow as one action."
    - "Raw gh GraphQL fallback is no longer the default path when Forge is available."
  artifacts:
    - path: src/services/assistants/summonables.ts
      provides: tightened agent routing and anti-fallback guidance
      min_lines: 70
    - path: src/services/assistants/copilot.ts
      provides: updated installed Copilot agent rendering if needed for the revised guidance
      min_lines: 50
    - path: tests/smoke/cli.test.ts
      provides: assertions on the new installed guidance
      min_lines: 95
  key_links:
    - from: src/services/assistants/summonables.ts
      to: src/services/assistants/copilot.ts
      via: rendered installed Copilot agent guidance
      pattern: Forge
---

<objective>
Tighten Copilot agent behavior so Forge remains the processor for discussion-analysis requests.

Purpose: The user selected `forge-discussion-analyzer`, so the agent should not improvise a different execution model unless Forge is actually unavailable.
Output: Installed agent guidance that keeps Copilot inside Forge-managed execution with one approval flow and explicit anti-fallback instructions.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-CONTEXT.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-RESEARCH.md
@/Users/ajitg/Downloads/feedback.log
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten installed agent routing and one-approval guidance</name>
  <files>src/services/assistants/summonables.ts, src/services/assistants/copilot.ts</files>
  <action>Update the installed Copilot agent guidance so it explicitly says Forge is the processor for GitHub Discussions requests, that the assistant should request approval once for the Forge command path, and that it should not switch to raw `gh api graphql` execution unless Forge is truly unavailable and the user chooses that fallback.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The installed Copilot agents make Forge the explicit processor and discourage raw GraphQL fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for the tightened installed guidance</name>
  <files>tests/smoke/cli.test.ts</files>
  <action>Add assertions on the generated Copilot agent files so the new guidance is preserved: Forge handles the request, one approval is expected, and raw GraphQL fallback is not the default path.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The repo has coverage for the revised installed Copilot guidance.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the tightened agent guidance
- [ ] Installed Copilot agents clearly route discussion-analysis requests through Forge
- [ ] Raw `gh api graphql` fallback is no longer the default path in the installed guidance
- [ ] `npm test` covers the revised guidance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The installed agents are disciplined about staying inside Forge-managed execution
- Approval behavior is clearer and less repetitive for the same request flow
  </success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-02-SUMMARY.md`
</output>
