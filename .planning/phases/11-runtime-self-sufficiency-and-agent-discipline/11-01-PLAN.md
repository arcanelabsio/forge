---
phase: 11-runtime-self-sufficiency-and-agent-discipline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/services/assistants/install.ts
  - tests/smoke/cli.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "The bundled ~/.copilot runtime is dependency-complete immediately after install."
    - "Forge does not require follow-on npm installs inside ~/.copilot/forge during normal use."
    - "The initial npx install resolves the runtime payload in full."
  artifacts:
    - path: src/services/assistants/install.ts
      provides: bundled runtime installation with dependency completeness
      min_lines: 100
    - path: package.json
      provides: packaging or runtime metadata needed for the bundled install model
      min_lines: 20
    - path: tests/smoke/cli.test.ts
      provides: verification that installed runtime commands work without post-install dependency repair
      min_lines: 90
  key_links:
    - from: src/services/assistants/install.ts
      to: package.json
      via: bundled runtime dependency resolution
      pattern: dependencies
---

<objective>
Make the installed ~/.copilot runtime dependency-complete during the initial npx install.

Purpose: The installed runtime cannot be considered self-sufficient if Copilot later has to run ad hoc `npm install` commands inside the Forge-owned subtree.
Output: A bundled runtime that is ready to execute immediately after installation.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-CONTEXT.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve bundled runtime dependencies during install</name>
  <files>src/services/assistants/install.ts, package.json, package-lock.json</files>
  <action>Refactor the bundled runtime installation under `~/.copilot/forge` so all required runtime dependencies are available immediately after `npx forge-ai-assist@latest`. Avoid designs that rely on later `npm install` repairs inside the runtime subtree during normal usage.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The installed runtime can execute Forge commands without later dependency installation prompts.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for dependency-complete runtime installs</name>
  <files>tests/smoke/cli.test.ts</files>
  <action>Add smoke coverage that verifies the installed runtime command under `~/.copilot/forge/bin/forge.mjs` executes after install without any post-install dependency repair. Keep the test isolated from the real home directory.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The repo has automated evidence that the bundled runtime is dependency-complete after installation.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the dependency-complete runtime install changes
- [ ] The installed runtime can execute immediately after install
- [ ] No post-install `npm install` is required under `~/.copilot/forge`
- [ ] `npm test` covers the dependency-complete install path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The bundled runtime is ready immediately after the initial installer run
- Copilot no longer needs to repair Forge dependencies during analysis
  </success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-01-SUMMARY.md`
</output>
