---
phase: 02-repository-analysis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/analysis.ts
  - src/config/analysis.ts
  - src/services/analysis/artifacts.ts
  - src/services/metadata.ts
autonomous: true
requirements:
  - ANLY-03
  - ANLY-04
user_setup: []
must_haves:
  truths:
    - "Repository analysis has a typed artifact contract with an explicit boundary between observed facts and inferred recommendations."
    - "Analysis outputs are durable sidecar artifacts, not transient console text."
    - "Forge can locate the latest analysis run without guessing from arbitrary filenames."
  artifacts:
    - path: src/contracts/analysis.ts
      provides: analysis run types and observed-vs-inferred schema
      min_lines: 40
      contains: observedFacts
    - path: src/services/analysis/artifacts.ts
      provides: analysis artifact pathing and persistence helpers
      min_lines: 35
      contains: latest
    - path: src/config/analysis.ts
      provides: sidecar analysis directory contract
      min_lines: 12
  key_links:
    - from: src/services/analysis/artifacts.ts
      to: src/contracts/analysis.ts
      via: typed analysis payload serialization
      pattern: AnalysisRun
    - from: src/services/analysis/artifacts.ts
      to: src/services/metadata.ts
      via: run index registration
      pattern: metadata
---

<objective>
Establish the artifact contract and persistence model for repository analysis.

Purpose: Phase 2 needs durable analysis outputs that later planning flows can consume without scraping console text.
Output: Typed analysis contracts, stable sidecar paths, and persistence helpers for analysis runs plus latest-run lookup.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-repository-analysis/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the repository-analysis domain model</name>
  <files>src/contracts/analysis.ts</files>
  <action>Create typed contracts for an analysis run, including repository identity, stack/structure summaries, assistant-context findings, `observedFacts`, and `recommendations`. Make the evidence boundary explicit in the schema so later planning logic cannot accidentally treat inferred guidance as observed repository state.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has one typed source of truth for repository-analysis payloads and evidence separation.</done>
</task>

<task type="auto">
  <name>Task 2: Add stable analysis artifact pathing and persistence helpers</name>
  <files>src/config/analysis.ts, src/services/analysis/artifacts.ts</files>
  <action>Introduce a dedicated sidecar subtree for analysis outputs and helpers that write a per-run JSON artifact plus a stable latest pointer or index. Reuse the sidecar contract from Phase 1 rather than inventing a parallel artifact root.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Analysis runs can be written and rediscovered through one stable sidecar contract.</done>
</task>

<task type="auto">
  <name>Task 3: Register analysis history in Forge metadata without bloating it</name>
  <files>src/services/metadata.ts, src/services/analysis/artifacts.ts</files>
  <action>Extend sidecar metadata so it can record lightweight analysis-run history or pointers, but keep the full payload in dedicated analysis artifact files. This preserves rerun history without turning `metadata.json` into the primary storage format for large analysis bundles.</action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "import('./dist/services/analysis/artifacts.js')"</automated>
  </verify>
  <done>Forge metadata can reference analysis runs while the durable payload lives in dedicated artifact files.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the new analysis contract and persistence modules
- [ ] `node -e "import('./dist/services/analysis/artifacts.js')"` loads after build
- [ ] The analysis schema contains distinct observed-fact and recommendation fields
- [ ] The sidecar contract includes both historical run storage and a stable latest-run lookup path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge has a durable, typed repository-analysis artifact model that later planning workflows can consume directly
  </success_criteria>

<output>
After completion, create `.planning/phases/02-repository-analysis/02-01-SUMMARY.md`
</output>
