---
phase: 02-repository-analysis
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/services/analysis/run.ts
  - src/commands/analyze.ts
  - src/program.ts
  - src/cli.ts
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
  - ANLY-04
user_setup: []
must_haves:
  truths:
    - "Forge exposes a user-facing analysis command that runs from the repo root and writes durable sidecar artifacts."
    - "Analysis output remains reviewable for humans without losing the underlying structured payload."
    - "Repeated analysis runs preserve prior outputs for later follow-up."
  artifacts:
    - path: src/services/analysis/run.ts
      provides: end-to-end analysis execution and persistence
      min_lines: 35
      contains: recommendations
    - path: src/commands/analyze.ts
      provides: CLI entrypoint for repository analysis
      min_lines: 25
    - path: src/program.ts
      provides: command registration for analysis
      min_lines: 30
  key_links:
    - from: src/commands/analyze.ts
      to: src/services/analysis/run.ts
      via: command execution
      pattern: analyze
    - from: src/services/analysis/run.ts
      to: src/services/analysis/artifacts.ts
      via: persisted sidecar outputs
      pattern: write
---

<objective>
Wire the repository-analysis pipeline into the CLI and persist reviewable outputs.

Purpose: Phase 2 needs a real user-facing workflow, not only internal services.
Output: An `analyze` command that executes the analyzers, derives recommendations, and writes durable sidecar artifacts for later planning.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-repository-analysis/02-RESEARCH.md
@.planning/phases/02-repository-analysis/02-01-PLAN.md
@.planning/phases/02-repository-analysis/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the end-to-end analysis run service</name>
  <files>src/services/analysis/run.ts</files>
  <action>Build the orchestration service that initializes the sidecar, invokes the repository analyzers, derives a bounded set of recommendations from observed facts, and persists both machine-readable artifacts and a human-reviewable summary. Keep the recommendation pass explicit so the observed/inferred boundary remains visible in both data and output.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can execute one complete analysis run and persist the result in the sidecar.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CLI analysis command</name>
  <files>src/commands/analyze.ts, src/program.ts</files>
  <action>Register a new repository-bound `analyze` command that runs the analysis service, prints a concise status summary, and points the user to the saved artifact locations. Reuse the existing Git/sidecar guardrails and error-reporting conventions rather than introducing a second CLI pattern.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js --help</automated>
  </verify>
  <done>The CLI exposes a first-class repository-analysis workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Prove rerun durability and reviewability</name>
  <files>src/services/analysis/run.ts, src/commands/analyze.ts, src/cli.ts</files>
  <action>Validate the command by running analysis twice against a temporary Git repository and confirming that prior run artifacts remain present while the latest pointer/index updates cleanly. Ensure the console output stays concise and references the saved artifact files instead of trying to dump the full analysis payload inline.</action>
  <verify>
    <automated>npm run build &amp;&amp; TMPDIR=$(mktemp -d) &amp;&amp; git -C "$TMPDIR" init &amp;&amp; cp package.json "$TMPDIR"/package.json &amp;&amp; node dist/cli.js analyze --cwd "$TMPDIR" &amp;&amp; node dist/cli.js analyze --cwd "$TMPDIR"</automated>
  </verify>
  <done>Analysis can be rerun safely, preserves prior outputs, and stays reviewable through saved sidecar artifacts.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the new analysis command
- [ ] `node dist/cli.js --help` shows the analysis command
- [ ] Running analysis writes structured artifacts and a human-reviewable summary into `.forge`
- [ ] Re-running analysis preserves prior outputs and updates a stable latest pointer/index
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge now has a complete repository-analysis workflow that persists reviewable artifacts for later planning
  </success_criteria>

<output>
After completion, create `.planning/phases/02-repository-analysis/02-03-SUMMARY.md`
</output>
