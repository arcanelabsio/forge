---
phase: 02-repository-analysis
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/services/analysis/repository.ts
  - src/services/analysis/stack.ts
  - src/services/analysis/structure.ts
  - src/services/analysis/assistant-context.ts
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
user_setup: []
must_haves:
  truths:
    - "Analysis always scopes itself from the resolved Git repository root."
    - "Forge inventories repository stack, structure, and assistant-relevant context needed for planning."
    - "Each analyzer returns evidence-backed findings that can feed the observed-facts section."
  artifacts:
    - path: src/services/analysis/repository.ts
      provides: root-scoped repository analysis orchestration
      min_lines: 30
      contains: repoRoot
    - path: src/services/analysis/stack.ts
      provides: technology stack inspection
      min_lines: 25
    - path: src/services/analysis/structure.ts
      provides: filesystem and module structure inventory
      min_lines: 25
    - path: src/services/analysis/assistant-context.ts
      provides: assistant-relevant repository signal detection
      min_lines: 25
  key_links:
    - from: src/services/analysis/repository.ts
      to: src/services/git.ts
      via: canonical repo-root resolution
      pattern: getRepoRoot
    - from: src/services/analysis/repository.ts
      to: src/contracts/analysis.ts
      via: typed analyzer output aggregation
      pattern: observedFacts
---

<objective>
Implement the repository analyzers that collect evidence-backed inputs for planning.

Purpose: Phase 2 must inventory the repository from the Git root and convert raw repository signals into typed analysis sections.
Output: Analyzer services for repository identity, stack, structure, and assistant-relevant context.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-repository-analysis/02-RESEARCH.md
@.planning/phases/02-repository-analysis/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a root-scoped repository analysis orchestrator</name>
  <files>src/services/analysis/repository.ts</files>
  <action>Create a service that accepts a working directory, resolves the canonical Git root through the existing Git service, and coordinates the domain analyzers from that root. Do not let downstream analyzers re-derive scope from `process.cwd()` because nested invocation must remain stable.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can start analysis from any nested directory and still analyze the same repository root.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stack and structure inventory analyzers</name>
  <files>src/services/analysis/stack.ts, src/services/analysis/structure.ts</files>
  <action>Add analyzers that inspect package manifests, lockfiles, workspace markers, top-level directories, and other repository signals needed to summarize the repository stack and structure. Keep outputs evidence-backed and concise rather than trying to infer implementation plans here.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can describe the repo’s technology stack and structural shape from checked-in evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Implement assistant-context detection for future planning</name>
  <files>src/services/analysis/assistant-context.ts</files>
  <action>Detect repository signals relevant to AI-assistant workflows such as prompt/config folders, `.github` assistant hints, existing automation/task files, and other summonable-entry clues. Return them as structured observations so Phase 3 planning can ground plans in repository reality instead of generic templates.</action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "import('./dist/services/analysis/repository.js')"</automated>
  </verify>
  <done>The analysis pipeline can surface assistant-relevant repository context as evidence-backed observations.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the new analyzer services
- [ ] Running the analysis orchestrator from a nested directory still resolves the same repo root
- [ ] Analyzer output includes stack, structure, and assistant-context sections
- [ ] Analyzer results remain evidence-backed and feed the observed-facts contract cleanly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge can inventory the repository from its Git root and produce the core observed inputs required for planning
  </success_criteria>

<output>
After completion, create `.planning/phases/02-repository-analysis/02-02-SUMMARY.md`
</output>
