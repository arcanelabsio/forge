---
phase: 06-milestone-audit-closure
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/phases/01-bootstrap-cli/01-VERIFICATION.md
autonomous: true
requirements: [INVK-01, DELV-01]

must_haves:
  truths:
    - "Forge packages correctly as a tarball including all dist files"
    - "Forge CLI can be invoked directly from the packed tarball via npx"
  artifacts:
    - path: ".planning/phases/01-bootstrap-cli/01-VERIFICATION.md"
      provides: "Packaging and invocation evidence"
  key_links: []
---

<objective>
Verify that the project correctly packages as an npm module and that the CLI can be invoked via `npx` from a packed tarball. This ensures the delivery foundational requirements (INVK-01 and DELV-01) are satisfied.

Purpose: Demonstrate real delivery and invocation capability.
Output: Evidence of successful packaging and invocation in the Phase 1 verification report.
</objective>

<execution_context>
@/Users/ajitg/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@package.json
@.planning/phases/01-bootstrap-cli/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Packaging and Invocation Verification</name>
  <files>None</files>
  <action>
    Perform a manual-style automated verification of the packaging and npx invocation.
    
    Commands:
    1. `npm run build` (ensure latest code is built)
    2. `npm pack` (generate the .tgz tarball)
    3. `mkdir -p temp-install && cd temp-install && npm init -y` (setup temp environment)
    4. `npx ../forge-ai-assist-0.1.0.tgz --help` (run CLI from tarball)
    
    Capture the output of the final command. It should show the Forge CLI help message.
    Also, verify the contents of the tarball (e.g., using `tar -tf forge-ai-assist-0.1.0.tgz`) to ensure `dist/cli.js` and other files are included.
  </action>
  <verify>npx command returns 0 and prints help message; tarball contains dist/ files</verify>
  <done>Packaging and invocation successfully demonstrated</done>
</task>

<task type="auto">
  <name>Task 2: Append Packaging Evidence to Verification Report</name>
  <files>.planning/phases/01-bootstrap-cli/01-VERIFICATION.md</files>
  <action>
    Append the evidence from Task 1 to `.planning/phases/01-bootstrap-cli/01-VERIFICATION.md`.
    - INVK-01: User can run Forge through npx forge-ai-assist@latest -> Mapping to successful `npx ./tarball.tgz` execution.
    - DELV-01: Project ships as an npm package with CLI -> Mapping to `package.json` configuration and `npm pack` verification results.
    
    Include the raw output of the `npx` command as evidence.
  </action>
  <verify>VERIFICATION.md contains entries for INVK-01 and DELV-01 with captured evidence</verify>
  <done>Phase 1 verification report is complete with all required evidence</done>
</task>

</tasks>

<verification>
- Verify that `01-VERIFICATION.md` contains entries for [INVK-01, DELV-01].
- Verify that the captured evidence shows the help message being printed from the packed tarball.
</verification>

<success_criteria>
- Verified delivery foundations: the project is publishable and runnable as an npm package.
- All Phase 1 requirements have completed auditable evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-milestone-audit-closure/06-02-SUMMARY.md`
</output>
