---
phase: 09-release-management-and-public-install
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/cli.ts
  - src/program.ts
  - tests/smoke/cli.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Forge publishes with metadata suitable for public discovery and installation."
    - "The public install contract remains a single command that works with standard Node tooling."
    - "The packaged artifact still exposes the `forge` binary from the `forge-ai-assist` package."
  artifacts:
    - path: package.json
      provides: publish-facing package metadata and install contract
      min_lines: 20
      contains: forge-ai-assist
    - path: tests/smoke/cli.test.ts
      provides: regression coverage for packaged installation behavior
      min_lines: 40
    - path: src/program.ts
      provides: stable runtime path for packaged installs
      min_lines: 30
  key_links:
    - from: package.json
      to: src/cli.ts
      via: published forge binary entrypoint
      pattern: forge
    - from: tests/smoke/cli.test.ts
      to: package.json
      via: packed artifact verification
      pattern: npm pack
---

<objective>
Harden Forge's package contract so it is ready for public release and one-command installation.

Purpose: Release automation is only credible if the package metadata, packed artifact, and public install story are already correct.
Output: Publish-ready package metadata plus automated verification of the packed install contract.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-release-management-and-public-install/09-CONTEXT.md
@.planning/phases/09-release-management-and-public-install/09-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add publish-facing package metadata and release prerequisites</name>
  <files>package.json, package-lock.json</files>
  <action>Add or refine the package metadata required for public publication and discovery, including repository-facing fields, license metadata, publish configuration, and any release scripts or validation hooks needed to support a repeatable release flow. Keep the package identity `forge-ai-assist` and the published executable `forge` unless a hard blocker is discovered.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The package manifest is publication-ready and explicit about Forge's install contract.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the packed artifact can still be installed and invoked cleanly</name>
  <files>tests/smoke/cli.test.ts, package.json, src/cli.ts, src/program.ts</files>
  <action>Extend smoke coverage so Forge validates the packed tarball, installation path, and binary invocation expected for public users. Prefer a realistic temporary install flow over synthetic manifest-only assertions, and keep the validation sidecar-safe.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The repo has automated evidence that the published artifact still supports the intended one-command install or run path.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with publish-facing metadata in place
- [ ] `package.json` is suitable for public release discovery and installation
- [ ] The package still exposes `forge` from the `forge-ai-assist` package
- [ ] `npm test` covers at least one realistic packed install path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The public install contract is explicit and preserved
- Forge's package artifact is ready to be consumed by an automated release workflow
  </success_criteria>

<output>
After completion, create `.planning/phases/09-release-management-and-public-install/09-01-SUMMARY.md`
</output>
