---
phase: 09-release-management-and-public-install
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - package.json
  - scripts/release-local.mjs
  - docs/releasing.md
autonomous: true
requirements:
  - EXTD-03
user_setup:
  - npm authentication configured on the local machine
must_haves:
  truths:
    - "Forge can be released from the maintainer's local machine."
    - "Publishing is gated by automated validation before release artifacts go public."
    - "The workflow can produce a versioned Forge release without relying on ad hoc manual steps."
  artifacts:
    - path: scripts/release-local.mjs
      provides: local scripted release workflow
      min_lines: 40
    - path: docs/releasing.md
      provides: maintainer-facing setup and operation notes for publishing
      min_lines: 25
  key_links:
    - from: scripts/release-local.mjs
      to: package.json
      via: build, test, pack, and publish steps
      pattern: npm
    - from: docs/releasing.md
      to: scripts/release-local.mjs
      via: documented local release procedure
      pattern: release-local
---

<objective>
Add local release tooling that validates Forge and publishes versioned releases.

Purpose: Maintainers need a release path that is auditable and repeatable without paying for hosted CI execution.
Output: Local release tooling plus the repository setup notes required to publish safely.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-release-management-and-public-install/09-CONTEXT.md
@.planning/phases/09-release-management-and-public-install/09-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a local validation and release command</name>
  <files>scripts/release-local.mjs, package.json</files>
  <action>Create a local release command that runs the release checks needed before publication, including clean-worktree validation, build, test, and package validation. Keep a dry-run mode so maintainers can verify a release candidate before publishing.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has a local release command suitable for gating publication.</done>
</task>

<task type="auto">
  <name>Task 2: Add publish orchestration and maintainer setup notes</name>
  <files>scripts/release-local.mjs, package.json, docs/releasing.md</files>
  <action>Extend the local release command so it can perform the publish step after validations pass, including versioning or tagging guidance where needed. Document the local-machine prerequisites, npm authentication expectations, and the exact publish path maintainers should use.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The repo contains a local release workflow capable of producing a versioned Forge release.</done>
</task>

</tasks>

<verification>
- [ ] A local release script exists for validation and publish orchestration
- [ ] The release workflow validates Forge before publishing
- [ ] The release workflow is compatible with local npm publication
- [ ] Required maintainer setup is documented clearly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Forge can be released from a local scripted flow instead of an ad hoc one-off process
- Publishing prerequisites are explicit and reviewable
  </success_criteria>

<output>
After completion, create `.planning/phases/09-release-management-and-public-install/09-02-SUMMARY.md`
</output>
