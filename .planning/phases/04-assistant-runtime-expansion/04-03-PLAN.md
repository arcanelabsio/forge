---
phase: 04-assistant-runtime-expansion
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/services/assistants/install.ts
  - src/commands/install-assistants.ts
  - src/program.ts
  - src/cli.ts
autonomous: true
requirements:
  - INVK-02
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-04
  - ASST-05
user_setup: []
must_haves:
  truths:
    - "Forge can install or update assistant-owned summonable entries from one managed source of truth."
    - "Unsupported or unavailable assistant contexts no-op safely with explicit reasons."
    - "The generalized install/update flow preserves prior valid state instead of damaging it on unsupported targets."
  artifacts:
    - path: src/services/assistants/install.ts
      provides: multi-assistant install/update orchestration
      min_lines: 35
      contains: no-op
    - path: src/commands/install-assistants.ts
      provides: CLI entrypoint for generalized assistant installation
      min_lines: 25
    - path: src/program.ts
      provides: assistant install/update command registration
      min_lines: 40
  key_links:
    - from: src/commands/install-assistants.ts
      to: src/services/assistants/install.ts
      via: shared multi-assistant orchestration
      pattern: install
    - from: src/services/assistants/install.ts
      to: src/services/assistants/registry.ts
      via: adapter discovery and availability checks
      pattern: registry
---

<objective>
Add the generalized install/update workflow and safe no-op behavior across assistants.

Purpose: Phase 4 completes when one shared Forge command can manage assistant runtime installs safely across the supported assistant set.
Output: A registry-driven install/update service and CLI command with explicit no-op behavior for unsupported or unavailable assistants.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-assistant-runtime-expansion/04-RESEARCH.md
@.planning/phases/04-assistant-runtime-expansion/04-01-PLAN.md
@.planning/phases/04-assistant-runtime-expansion/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared assistant install/update orchestrator</name>
  <files>src/services/assistants/install.ts</files>
  <action>Implement a multi-assistant install/update service that iterates the adapter registry, renders native assets from the shared source definition, and writes or updates assistant-owned runtime targets. Return structured results for installed, updated, skipped, and no-op states so the CLI can report behavior clearly.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has one orchestration path for managing assistant runtime installs across the supported set.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit safe no-op behavior for unsupported or unavailable contexts</name>
  <files>src/services/assistants/install.ts, src/cli.ts</files>
  <action>Make availability checks produce explicit safe no-op results when an assistant target is unsupported or unavailable in the current environment. Do not treat these cases as destructive failures, and do not remove prior valid installations just because a later update attempt cannot proceed.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Unsupported assistant targets no-op safely and preserve prior installation state.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the generalized assistant install/update command</name>
  <files>src/commands/install-assistants.ts, src/program.ts</files>
  <action>Add a user-facing command that installs or updates one or more assistant targets from the shared source model and prints a per-assistant summary of installed, updated, skipped, or no-op results. Keep the command compatible with the Copilot proof while expanding to the broader assistant set.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js --help</automated>
  </verify>
  <done>The CLI exposes one shared assistant install/update flow that reports safe no-op behavior clearly.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the shared assistant install/update workflow
- [ ] `node dist/cli.js --help` shows the generalized assistant install/update command
- [ ] Unsupported or unavailable assistant targets produce explicit safe no-op results
- [ ] Prior valid installation state is preserved when a target cannot be updated
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge can manage summonable assistant entries across Claude, GitHub Copilot, Codex, and Gemini from one shared source with safe no-op handling
  </success_criteria>

<output>
After completion, create `.planning/phases/04-assistant-runtime-expansion/04-03-SUMMARY.md`
</output>
