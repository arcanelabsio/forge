---
phase: 04-assistant-runtime-expansion
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/services/assistants/claude.ts
  - src/services/assistants/copilot.ts
  - src/services/assistants/codex.ts
  - src/services/assistants/gemini.ts
  - src/assets/assistants/
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-05
user_setup: []
must_haves:
  truths:
    - "Each supported assistant receives a native summonable entrypoint generated from one shared source definition."
    - "Forge can install or update assistant-owned runtime assets without requiring manual copy steps."
    - "The Copilot proof remains one adapter inside the generalized runtime model."
  artifacts:
    - path: src/services/assistants/claude.ts
      provides: Claude adapter and install target rules
      min_lines: 25
    - path: src/services/assistants/codex.ts
      provides: Codex adapter and install target rules
      min_lines: 25
    - path: src/services/assistants/gemini.ts
      provides: Gemini adapter and install target rules
      min_lines: 25
    - path: src/assets/assistants/
      provides: shared assistant-native entry templates
  key_links:
    - from: src/services/assistants/claude.ts
      to: src/services/assistants/render-entry.ts
      via: shared content rendering
      pattern: render
    - from: src/services/assistants/copilot.ts
      to: src/services/assistants/registry.ts
      via: generalized adapter registration
      pattern: copilot
---

<objective>
Implement the assistant-specific adapters and native runtime assets for all supported assistants.

Purpose: Phase 4 must move beyond the Copilot proof and provide native summonable entries across the required assistant set.
Output: Adapter implementations and native entry assets for Claude, GitHub Copilot, Codex, and Gemini.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-assistant-runtime-expansion/04-RESEARCH.md
@.planning/phases/04-assistant-runtime-expansion/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement native adapters for Claude, Codex, and Gemini</name>
  <files>src/services/assistants/claude.ts, src/services/assistants/codex.ts, src/services/assistants/gemini.ts</files>
  <action>Build assistant adapters that can render the shared summonable entry model into the native conventions expected by Claude, Codex, and Gemini. Reuse the shared renderer and registry contracts instead of cloning the Copilot proof structure into three independent implementations.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can prepare native summonable runtime assets for all non-Copilot v1 assistants.</done>
</task>

<task type="auto">
  <name>Task 2: Fold the Copilot proof into the generalized adapter set</name>
  <files>src/services/assistants/copilot.ts, src/assets/assistants/</files>
  <action>Refactor the Phase 3 Copilot adapter as one member of the generalized assistant set and move any shared asset content into a common assistant-assets subtree. Preserve the existing `/agent` proof behavior while aligning it with the shared adapter/renderer contract.</action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "import('./dist/services/assistants/copilot.js')"</automated>
  </verify>
  <done>The Copilot proof now lives inside the shared runtime system without losing native behavior.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with all assistant adapters
- [ ] Claude, GitHub Copilot, Codex, and Gemini each have a native adapter path
- [ ] The shared entry definition drives all rendered assistant outputs
- [ ] No assistant requires manual copying of the shared Forge entry content
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge can render and install native summonable entries for all required assistants from one managed source
  </success_criteria>

<output>
After completion, create `.planning/phases/04-assistant-runtime-expansion/04-02-SUMMARY.md`
</output>
