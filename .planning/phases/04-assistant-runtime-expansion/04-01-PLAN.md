---
phase: 04-assistant-runtime-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/assistants.ts
  - src/contracts/summonable-entry.ts
  - src/services/assistants/registry.ts
  - src/services/assistants/render-entry.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
user_setup: []
must_haves:
  truths:
    - "Forge has one shared internal model for summonable assistant entries."
    - "Assistant-specific behavior is represented through a registry of adapters rather than duplicated workflow logic."
    - "The shared model can support Claude, GitHub Copilot, Codex, and Gemini."
  artifacts:
    - path: src/contracts/summonable-entry.ts
      provides: assistant-agnostic summonable entry model
      min_lines: 30
      contains: prompt
    - path: src/contracts/assistants.ts
      provides: supported assistant identifiers and capability contracts
      min_lines: 25
    - path: src/services/assistants/registry.ts
      provides: adapter registry for supported assistants
      min_lines: 25
      contains: copilot
  key_links:
    - from: src/services/assistants/registry.ts
      to: src/contracts/assistants.ts
      via: adapter contract registration
      pattern: AssistantId
    - from: src/services/assistants/render-entry.ts
      to: src/contracts/summonable-entry.ts
      via: shared entry rendering
      pattern: SummonableEntry
---

<objective>
Define the shared assistant runtime model and adapter registry.

Purpose: Phase 4 depends on a common internal representation before it can expand the Copilot proof into multi-assistant support.
Output: Typed assistant contracts, a summonable-entry model, and a registry-driven adapter layer.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-copilot-planning-proof/03-RESEARCH.md
@.planning/phases/04-assistant-runtime-expansion/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the shared summonable-entry model</name>
  <files>src/contracts/summonable-entry.ts, src/contracts/assistants.ts</files>
  <action>Create a shared internal model for Forge’s summonable entry content, metadata, and task identity, plus assistant capability contracts for Claude, GitHub Copilot, Codex, and Gemini. Keep the model narrow to what installation and invocation need instead of trying to encode every future assistant behavior.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has one assistant-agnostic task/entry model that covers all required v1 assistants.</done>
</task>

<task type="auto">
  <name>Task 2: Build the registry-driven adapter layer</name>
  <files>src/services/assistants/registry.ts, src/services/assistants/render-entry.ts</files>
  <action>Implement a registry that maps each supported assistant to its renderer, install-target logic, and availability checks. The registry should be the only place that knows which assistants are supported and how their native presentation differs.</action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "import('./dist/services/assistants/registry.js')"</automated>
  </verify>
  <done>Assistant-specific behavior is isolated behind a shared registry rather than scattered across commands.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the shared assistant contracts and registry
- [ ] The supported assistant list includes Claude, GitHub Copilot, Codex, and Gemini
- [ ] The shared summonable-entry model remains assistant-agnostic while adapters define native presentation
- [ ] `node -e "import('./dist/services/assistants/registry.js')"` loads after build
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge has a shared runtime model that can drive all supported assistant adapters from one source of truth
  </success_criteria>

<output>
After completion, create `.planning/phases/04-assistant-runtime-expansion/04-01-SUMMARY.md`
</output>
