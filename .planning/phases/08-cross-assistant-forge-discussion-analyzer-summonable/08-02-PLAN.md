---
phase: 08-cross-assistant-forge-discussion-analyzer-summonable
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/services/discussions
  - src/contracts/discussions.ts
  - src/program.ts
  - tests
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Forge owns the heavy discussion-analysis runtime behavior instead of embedding it in installed assistant assets."
    - "Raw discussions are transformed into compact, analysis-ready sidecar artifacts before model-facing analysis runs."
    - "The resulting workflow preserves the behavioral intent of the attached gh-discussion-analyzer agent while using less prompt context."
  artifacts:
    - path: src/services/discussions
      provides: Forge-managed preprocessing and analysis runtime for discussion digests
      min_lines: 1
    - path: src/program.ts
      provides: execution path for the discussion analyzer runtime
      min_lines: 60
    - path: tests
      provides: coverage for compact digest generation and runtime invocation
      min_lines: 1
  key_links:
    - from: src/program.ts
      to: src/services/discussions
      via: discussion analyzer execution path
      pattern: discussion
    - from: src/services/discussions
      to: src/contracts/discussions.ts
      via: raw discussion data transformed into compact analysis artifacts
      pattern: discussion
---

<objective>
Build the Forge-managed discussion-analysis runtime and compact context pipeline behind `forge-discussion-analyzer`.

Purpose: The installed summonable should stay small; the real analysis power needs to live in Forge runtime code and sidecar artifacts.
Output: A Forge-managed preprocessing and analysis pipeline that turns fetched discussions into compact, question-ready artifacts and reports.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-CONTEXT.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-RESEARCH.md
@/Users/ajitg/Downloads/gh-discussion-analyzer.agent.md
@src/services/discussions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact discussion-analysis artifacts and preprocessing</name>
  <files>src/services/discussions, src/contracts/discussions.ts</files>
  <action>Implement a Forge-side preprocessing layer that reads raw `.forge/discussions` fetch artifacts, extracts the signal needed by the attached analyzer behavior, and writes compact, reviewable analysis-ready artifacts. Preserve traceability between raw discussions, extracted summaries, and derived analysis context.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can prepare compact discussion-analysis context without forcing assistants to re-ingest the raw discussions corpus every time.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Forge-managed discussion analyzer runtime path</name>
  <files>src/program.ts, src/services/discussions</files>
  <action>Implement the runtime entry that powers `forge-discussion-analyzer`: it should accept a user question, load or prepare the compact artifacts, and generate the digest/reporting behavior currently described in the attached `.agent.md`. Keep the heavy orchestration in Forge and expose only a compact assistant-facing contract.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge provides a real backend for discussion analysis instead of relying on a long installed prompt file.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for preprocessing and runtime behavior</name>
  <files>tests</files>
  <action>Add automated tests that cover compact artifact generation, question-oriented runtime invocation, and key report-shape guarantees from the attached analyzer behavior. The tests should prove that analysis can run from prepared sidecar artifacts without the full legacy prompt body.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The compact discussion-analysis pipeline is covered by automated tests.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the discussion analyzer runtime
- [ ] Forge can transform raw discussions into compact analysis-ready artifacts
- [ ] The discussion analyzer runtime can answer a user question from prepared sidecar context
- [ ] `npm test` covers preprocessing and runtime behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No writes occur outside Forge-managed assistant targets and the `.forge` sidecar
- Forge owns the operational analysis logic that used to live inside the bulky legacy agent file
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-02-SUMMARY.md`
</output>
