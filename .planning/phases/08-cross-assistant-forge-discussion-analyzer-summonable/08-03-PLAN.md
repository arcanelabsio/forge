---
phase: 08-cross-assistant-forge-discussion-analyzer-summonable
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - src/services/assistants
  - tests
  - .planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Users can invoke forge-discussion-analyzer through the closest native summonable surface on each supported assistant."
    - "Copilot gets the explicit select-and-ask experience, and other assistants get a documented equivalent."
    - "Any unavoidable assistant-specific degradation is explicit, tested, and does not hide behind generic messaging."
  artifacts:
    - path: src/services/assistants
      provides: assistant-native invocation wrappers for forge-discussion-analyzer
      min_lines: 1
    - path: tests
      provides: end-to-end verification of installed summonable behavior per assistant
      min_lines: 1
    - path: .planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable
      provides: summary evidence describing platform-specific invocation parity or degradation
      min_lines: 1
  key_links:
    - from: src/services/assistants
      to: src/program.ts
      via: assistant-native invocation path into the Forge analyzer runtime
      pattern: discussion-analyzer
    - from: src/services/assistants
      to: src/services/discussions
      via: installed summonable calls the compact analysis backend
      pattern: discussion
---

<objective>
Wire assistant-native invocation flows for `forge-discussion-analyzer` and verify cross-assistant summonability.

Purpose: The feature is only complete if users can actually select or invoke `forge-discussion-analyzer` from their assistant runtime and get the expected Forge-backed behavior.
Output: Assistant-specific summonable wrappers, verified invocation flows, and explicit documentation of any platform-specific limitations.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-CONTEXT.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-RESEARCH.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-01-PLAN.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize assistant-native invocation wrappers</name>
  <files>src/services/assistants</files>
  <action>Update assistant adapters and rendered summonable assets so `forge-discussion-analyzer` is installed with the closest native invocation shape each platform supports. For Copilot, explicitly target the `/agent` selection mental model. For other assistants, install the closest equivalent while preserving the same capability identity and runtime contract.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Each supported assistant gets a native wrapper for invoking `forge-discussion-analyzer`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end summonability across assistants</name>
  <files>tests</files>
  <action>Add end-to-end verification that installed assets for each assistant can route into the Forge-managed discussion analyzer runtime with the expected naming, invocation instructions, and sidecar contract. Focus on installed artifact shape, routing semantics, and no-op/degradation behavior rather than pretending every assistant has identical UI affordances.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The repo has evidence that `forge-discussion-analyzer` is summonable across the supported assistant set.</done>
</task>

<task type="auto">
  <name>Task 3: Record platform-specific parity and limitations</name>
  <files>.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable</files>
  <action>Document the final cross-assistant invocation model, including where Copilot gets exact `/agent` parity and where other assistants receive the closest available equivalent. Capture any degradation explicitly so future phases can improve it instead of burying it.</action>
  <verify>
    <automated>test -f .planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-03-SUMMARY.md || true</automated>
  </verify>
  <done>The phase leaves behind explicit evidence of how `forge-discussion-analyzer` maps to each assistant runtime.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with assistant-native invocation wrappers
- [ ] Installed assets expose `forge-discussion-analyzer` with explicit invocation guidance per assistant
- [ ] Copilot receives the desired select-and-ask experience, and other assistants receive a documented equivalent
- [ ] `npm test` covers cross-assistant summonability and degradation behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No writes occur outside Forge-managed assistant targets and the `.forge` sidecar
- Users can summon `forge-discussion-analyzer` from supported assistant environments without pasting the full legacy `.agent.md` prompt body
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-03-SUMMARY.md`
</output>
