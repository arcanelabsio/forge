---
phase: 08-cross-assistant-forge-discussion-analyzer-summonable
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/summonable-entry.ts
  - src/services/assistants/install.ts
  - src/services/assistants/registry.ts
  - src/services/assistants/forge-agent.ts
  - src/program.ts
  - tests
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Forge can install multiple named summonables instead of only one generic entry."
    - "forge-discussion-analyzer is a first-class installed summonable across supported assistant runtimes."
    - "Assistant installation remains centralized even as Forge adds more summonable capabilities."
  artifacts:
    - path: src/contracts/summonable-entry.ts
      provides: model support for multiple named summonables and richer invocation metadata
      min_lines: 25
    - path: src/services/assistants/install.ts
      provides: installation flow that can materialize more than one summonable asset
      min_lines: 60
    - path: src/services/assistants/forge-agent.ts
      provides: named forge-discussion-analyzer summonable definition
      min_lines: 25
    - path: tests
      provides: coverage for named summonable installation behavior
      min_lines: 1
  key_links:
    - from: src/program.ts
      to: src/services/assistants/install.ts
      via: installation flow for named summonables
      pattern: install
    - from: src/services/assistants/install.ts
      to: src/services/assistants/registry.ts
      via: assistant-specific rendering of multiple summonables
      pattern: registry
---

<objective>
Extend Forge from one generic assistant entry to a named summonable model that can install `forge-discussion-analyzer` across assistants.

Purpose: The discussion analyzer cannot exist as a mere variation inside one generic Forge file; it must become its own installed capability with a stable name and invocation contract.
Output: A multi-entry summonable installation model with `forge-discussion-analyzer` available as a distinct installed asset.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-CONTEXT.md
@.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-RESEARCH.md
@/Users/ajitg/Downloads/gh-discussion-analyzer.agent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a named summonable registry model</name>
  <files>src/contracts/summonable-entry.ts, src/services/assistants/forge-agent.ts</files>
  <action>Refactor the summonable model so Forge can define and ship multiple named capabilities rather than one generic agent blob. Add a first-class summonable definition for `forge-discussion-analyzer`, preserving the behavioral purpose of the attached legacy agent while keeping the installed content compact.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has an internal model that can represent `forge-discussion-analyzer` as a distinct summonable capability.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the installer to materialize named summonables across assistants</name>
  <files>src/services/assistants/install.ts, src/services/assistants/registry.ts, src/program.ts</files>
  <action>Extend the assistant installation flow so it can install one or more named summonables per assistant runtime, including `forge-discussion-analyzer`. Keep the install UX centralized in Forge and ensure assistant-specific rendering/paths remain adapter-driven.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can install the named discussion analyzer summonable through the shared assistant runtime path.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for named summonable installation</name>
  <files>tests</files>
  <action>Add automated tests covering the new multi-summonable install path, including assertions that `forge-discussion-analyzer` is installed with the expected naming and that existing assistant installation behavior does not regress.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The named summonable install model is covered by automated tests.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with named summonable installation support
- [ ] Forge can install `forge-discussion-analyzer` as a distinct assistant asset
- [ ] Existing assistant installation behavior still works for the rest of the install flow
- [ ] `npm test` covers named summonable installation behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No writes occur outside Forge-managed assistant targets and the `.forge` sidecar
- Forge can install `forge-discussion-analyzer` across supported assistant runtimes as a first-class named capability
  </success_criteria>

<output>
After completion, create `.planning/phases/08-cross-assistant-forge-discussion-analyzer-summonable/08-01-SUMMARY.md`
</output>
