---
phase: 03-copilot-planning-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contracts/planning.ts
  - src/config/planning.ts
  - src/services/planning/artifacts.ts
  - src/services/planning/generator.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-04
user_setup: []
must_haves:
  truths:
    - "Forge has a typed action-plan model that is driven by repository analysis artifacts."
    - "Generated plans remain explicit and reviewable rather than implying automatic code changes."
    - "Plan history is preserved as durable sidecar artifacts."
  artifacts:
    - path: src/contracts/planning.ts
      provides: plan-run and action-plan schema
      min_lines: 35
      contains: analysisRunId
    - path: src/services/planning/generator.ts
      provides: analysis-driven planning engine
      min_lines: 35
      contains: recommendations
    - path: src/services/planning/artifacts.ts
      provides: plan artifact persistence and history lookup
      min_lines: 30
      contains: latest
  key_links:
    - from: src/services/planning/generator.ts
      to: src/contracts/analysis.ts
      via: analysis artifact consumption
      pattern: AnalysisRun
    - from: src/services/planning/artifacts.ts
      to: src/contracts/planning.ts
      via: persisted plan-run schema
      pattern: PlanRun
---

<objective>
Create the core planning engine and durable plan artifact model.

Purpose: Phase 3 needs a reusable planning core before adding any Copilot-specific entrypoint.
Output: Typed plan contracts, a generator that consumes repository analysis, and persistent plan-run storage with history.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-repository-analysis/02-RESEARCH.md
@.planning/phases/03-copilot-planning-proof/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define typed planning contracts linked to analysis runs</name>
  <files>src/contracts/planning.ts, src/config/planning.ts</files>
  <action>Create typed contracts for plan runs, individual action-plan items, plan presentation metadata, and source linkage back to the analysis run they were generated from. Include fields that make reviewability explicit, such as assumptions, evidence references, and a statement that plans are advisory rather than automatically executed.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has one typed planning model with explicit analysis lineage and reviewability metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Build the analysis-driven planning generator</name>
  <files>src/services/planning/generator.ts</files>
  <action>Implement a planning generator that consumes a saved analysis artifact and produces specific action plans grounded in observed repository facts plus bounded recommendations. Keep the generator deterministic enough that repeated runs on the same inputs are comparable, even if later prompts or presentation vary.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The planning engine can turn repository analysis into structured action plans without rescanning the repository as its primary input.</done>
</task>

<task type="auto">
  <name>Task 3: Persist immutable plan-run history in the sidecar</name>
  <files>src/services/planning/artifacts.ts</files>
  <action>Add sidecar persistence for plan runs, including historical run storage and a stable latest pointer. Record the source analysis run ID and enough metadata to compare plan generations later, but do not overwrite older plan outputs.</action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "import('./dist/services/planning/artifacts.js')"</automated>
  </verify>
  <done>Plan outputs are durable, historical, and linked back to the analysis run that produced them.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the planning contracts and generator
- [ ] Planning artifacts include source analysis run IDs and preserved history
- [ ] Generated plans include explicit reviewability metadata and avoid execution assumptions
- [ ] `node -e "import('./dist/services/planning/artifacts.js')"` loads after build
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge has a durable, analysis-driven planning core ready for Copilot integration
  </success_criteria>

<output>
After completion, create `.planning/phases/03-copilot-planning-proof/03-01-SUMMARY.md`
</output>
