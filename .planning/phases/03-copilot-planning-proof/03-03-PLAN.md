---
phase: 03-copilot-planning-proof
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/services/planning/run.ts
  - src/commands/plan.ts
  - src/program.ts
  - src/cli.ts
autonomous: true
requirements:
  - INVK-03
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
user_setup: []
must_haves:
  truths:
    - "A user can invoke the first end-to-end planning workflow and receive reviewable plans grounded in saved analysis."
    - "Generated plans are associated with invocation context such as discussion or task identity."
    - "Repeated planning runs preserve prior outputs for later comparison."
  artifacts:
    - path: src/services/planning/run.ts
      provides: end-to-end plan execution orchestrator
      min_lines: 35
      contains: invocation
    - path: src/commands/plan.ts
      provides: CLI planning entrypoint
      min_lines: 25
    - path: src/program.ts
      provides: planning command registration
      min_lines: 35
  key_links:
    - from: src/commands/plan.ts
      to: src/services/planning/run.ts
      via: CLI workflow entry
      pattern: plan
    - from: src/services/planning/run.ts
      to: src/services/planning/artifacts.ts
      via: persisted plan-run outputs
      pattern: latest
---

<objective>
Wire the full planning proof end to end, including invocation context and run history.

Purpose: Phase 3 is only complete when the Copilot proof can trigger a grounded planning workflow and persist its outputs for later review.
Output: A plan-run orchestration service and CLI entrypoint that bind invocation context, analysis inputs, and historical plan artifacts together.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-copilot-planning-proof/03-RESEARCH.md
@.planning/phases/03-copilot-planning-proof/03-01-PLAN.md
@.planning/phases/03-copilot-planning-proof/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the end-to-end plan-run orchestrator</name>
  <files>src/services/planning/run.ts</files>
  <action>Build the orchestration service that loads the latest repository analysis, accepts invocation context such as requested task or discussion identifier, generates a plan set, and persists both structured and human-reviewable outputs. Fail clearly when no analysis artifact exists instead of silently falling back to generic templates.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can execute a complete planning run from saved analysis and contextual input.</done>
</task>

<task type="auto">
  <name>Task 2: Add the user-facing planning command</name>
  <files>src/commands/plan.ts, src/program.ts, src/cli.ts</files>
  <action>Expose a CLI planning command that accepts task/context input, routes into the planning orchestrator, and prints a concise summary of where the saved plan artifacts live. Keep user messaging explicit that Forge produced reviewable plans and did not mutate repository code automatically.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js --help</automated>
  </verify>
  <done>The CLI exposes a review-first planning workflow suitable for direct use or Copilot integration.</done>
</task>

<task type="auto">
  <name>Task 3: Verify plan history and invocation association</name>
  <files>src/services/planning/run.ts, src/commands/plan.ts</files>
  <action>Run the planning flow multiple times with different invocation metadata and confirm each result is preserved in sidecar history with links back to the source analysis run and invocation context. Ensure prior plan outputs remain available for comparison rather than being replaced in place.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js plan --task "baseline review" &amp;&amp; node dist/cli.js plan --task "follow-up review" --context discussion-2</automated>
  </verify>
  <done>Forge preserves reviewable plan history and associates outputs with the invocation that produced them.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the planning command and orchestration service
- [ ] Planning fails clearly if no repository-analysis artifact exists yet
- [ ] Generated plans are linked to source analysis plus invocation context
- [ ] Repeated planning runs preserve earlier outputs for later comparison
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge has a complete Copilot planning proof that turns repository analysis into stored, reviewable action plans
  </success_criteria>

<output>
After completion, create `.planning/phases/03-copilot-planning-proof/03-03-SUMMARY.md`
</output>
