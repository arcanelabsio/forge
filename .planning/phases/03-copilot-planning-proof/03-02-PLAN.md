---
phase: 03-copilot-planning-proof
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/services/assistants/copilot.ts
  - src/commands/install-copilot.ts
  - src/assets/copilot/agent.md
  - src/program.ts
autonomous: true
requirements:
  - INVK-03
  - PLAN-03
user_setup: []
must_haves:
  truths:
    - "Forge can expose a native GitHub Copilot `/agent` entrypoint as the first summoned workflow."
    - "The Copilot-specific surface is an adapter around the shared planning core rather than a one-off planning implementation."
    - "The entrypoint carries workflow context into the planning engine."
  artifacts:
    - path: src/services/assistants/copilot.ts
      provides: GitHub Copilot adapter/install logic
      min_lines: 30
      contains: /agent
    - path: src/commands/install-copilot.ts
      provides: CLI surface for Copilot proof installation
      min_lines: 25
    - path: src/assets/copilot/agent.md
      provides: native Copilot summonable entry content
      min_lines: 12
  key_links:
    - from: src/commands/install-copilot.ts
      to: src/services/assistants/copilot.ts
      via: installation/invocation wiring
      pattern: copilot
    - from: src/services/assistants/copilot.ts
      to: src/services/planning/generator.ts
      via: shared planning engine
      pattern: generate
---

<objective>
Add the GitHub Copilot-specific entrypoint and installation surface for the planning proof.

Purpose: Phase 3 must prove one native summoned workflow end to end, and GitHub Copilot is the first supported assistant.
Output: A Copilot adapter, install command, and native `/agent` entry asset wired to the shared planning engine.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-copilot-planning-proof/03-RESEARCH.md
@.planning/phases/03-copilot-planning-proof/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the GitHub Copilot adapter and native entry asset</name>
  <files>src/services/assistants/copilot.ts, src/assets/copilot/agent.md</files>
  <action>Create a dedicated Copilot adapter that knows how to materialize the first native `/agent` entrypoint for Forge. Keep the adapter responsible for Copilot-specific conventions, while delegating actual plan generation to the shared planning engine from Plan 03-01.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has a Copilot-native entry asset and adapter instead of baking Copilot rules into the planning core.</done>
</task>

<task type="auto">
  <name>Task 2: Expose a CLI install path for the Copilot proof</name>
  <files>src/commands/install-copilot.ts, src/program.ts</files>
  <action>Add a focused CLI command that installs or emits the Copilot proof entrypoint and reports where the native asset was written. Keep the command narrow to the proof workflow; Phase 4 will generalize install/update flows across assistants.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js --help</automated>
  </verify>
  <done>The CLI can expose the first Copilot-specific summonable workflow without entangling later multi-assistant work.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the Copilot adapter and install command
- [ ] `node dist/cli.js --help` shows the Copilot installation/proof command
- [ ] The Copilot entry asset is native to Copilot conventions and references the shared planning engine
- [ ] The adapter keeps assistant-specific behavior isolated from the planning core
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge can surface a GitHub Copilot `/agent` entrypoint as its first native summoned workflow
  </success_criteria>

<output>
After completion, create `.planning/phases/03-copilot-planning-proof/03-02-SUMMARY.md`
</output>
