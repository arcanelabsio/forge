---
phase: 10-copilot-runtime-bootstrap-and-install-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/program.ts
  - src/commands/install-assistants.ts
  - src/contracts/assistants.ts
  - tests/smoke/cli.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Forge makes the global Copilot install destination explicit."
    - "Forge no longer exposes repo-local or project-scoped Copilot installs."
    - "The public installer remains Copilot-first."
  artifacts:
    - path: src/program.ts
      provides: install flow that resolves the global Copilot install destination
      min_lines: 35
    - path: src/commands/install-assistants.ts
      provides: copilot-first installer messaging and install target reporting
      min_lines: 30
    - path: tests/smoke/cli.test.ts
      provides: coverage for global install-root behavior
      min_lines: 60
  key_links:
    - from: src/program.ts
      to: src/commands/install-assistants.ts
      via: user-facing installer flow
      pattern: installAssistantsCommand
---

<objective>
Design and implement the Copilot-first installer UX for the global `~/.copilot` destination.

Purpose: A fresh-machine installer needs to make the global runtime destination explicit before it can bootstrap anything reliably.
Output: A Copilot-focused install flow that targets `~/.copilot` and reports that destination clearly.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-CONTEXT.md
@.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the global Copilot install root explicit</name>
  <files>src/program.ts, src/commands/install-assistants.ts, src/contracts/assistants.ts</files>
  <action>Refactor the public install flow so Forge resolves an explicit global Copilot target root under `~/.copilot` instead of silently assuming the current working directory. Remove any implication of project-local installation from the public UX, and keep the install flow Copilot-only.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The installer makes it clear that Forge is targeting the global Copilot root under `~/.copilot`.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for the global install-root flow</name>
  <files>tests/smoke/cli.test.ts</files>
  <action>Add smoke coverage for the global install target so the installer proves both resolution and messaging for fresh-machine `~/.copilot` installs. Keep the tests sidecar-safe and avoid depending on the real home directory.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Install-root selection behavior is covered by automated smoke tests.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the install-root UX changes
- [ ] Forge resolves the explicit global Copilot target root under `~/.copilot`
- [ ] Installer output reports the target location clearly
- [ ] `npm test` covers the global install-root behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The user-facing installer makes install location explicit
- The public installer no longer implies project-local installation
- Copilot remains the only exposed assistant target
  </success_criteria>

<output>
After completion, create `.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-01-SUMMARY.md`
</output>
