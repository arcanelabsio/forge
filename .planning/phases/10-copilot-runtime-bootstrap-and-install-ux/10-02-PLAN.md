---
phase: 10-copilot-runtime-bootstrap-and-install-ux
plan: "02"
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/services/assistants/install.ts
  - src/services/assistants/copilot.ts
  - src/contracts/assistants.ts
  - src/services/metadata.ts
  - tests/smoke/cli.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Forge bootstraps missing Copilot runtime directories under `~/.copilot`."
    - "Forge installs a bundled tools/runtime payload under `~/.copilot` so agents do not depend on an external Forge binary."
    - "Forge writes installer-owned metadata so future installs can reason about existing state."
    - "Fresh-machine installs succeed even when the runtime layout starts empty."
  artifacts:
    - path: src/services/assistants/install.ts
      provides: runtime bootstrap, bundled tools installation, and installer-state handling
      min_lines: 80
    - path: src/services/assistants/copilot.ts
      provides: asset placement and bundled runtime invocation under Copilot conventions
      min_lines: 40
    - path: tests/smoke/cli.test.ts
      provides: fresh-machine global bootstrap coverage
      min_lines: 70
  key_links:
    - from: src/services/assistants/install.ts
      to: src/services/assistants/copilot.ts
      via: runtime bootstrap and asset installation
      pattern: getInstallTarget
---

<objective>
Bootstrap missing Copilot runtime structure, install bundled Forge tools, and record Forge-owned installer metadata.

Purpose: The first-run installer must succeed on machines where Copilot's runtime directories do not yet exist and where no separate Forge runtime is available on PATH.
Output: Idempotent global runtime bootstrap plus bundled tools and metadata that records what Forge created and installed.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-CONTEXT.md
@.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ~/.copilot runtime bootstrap before asset writes</name>
  <files>src/services/assistants/install.ts, src/services/assistants/copilot.ts, src/contracts/assistants.ts</files>
  <action>Refactor the installer to bootstrap the global Copilot runtime root under `~/.copilot` before writing agent assets. Create the minimal directory structure needed for `/agent` discovery and for a Forge-owned tools/runtime subtree, and keep bootstrap idempotent on reruns.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can install into an empty `~/.copilot` runtime root without manual directory setup.</done>
</task>

<task type="auto">
  <name>Task 2: Install bundled tools and persist runtime metadata</name>
  <files>src/services/assistants/install.ts, src/services/metadata.ts, tests/smoke/cli.test.ts</files>
  <action>Add a bundled Forge-owned tools/runtime payload under `~/.copilot` and a small metadata artifact that records Forge version, installed summonables, bundled tools, and last install state. Use that metadata to improve created-vs-updated reporting and future install reliability.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Forge installs bundled tools and writes reusable installer metadata when bootstrapping Copilot runtime state.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with runtime bootstrap logic
- [ ] Fresh installs succeed when `~/.copilot` starts empty
- [ ] Forge installs bundled tools and writes installer-owned metadata under `~/.copilot`
- [ ] `npm test` covers fresh-machine global bootstrap behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Fresh-machine installation no longer depends on preexisting runtime structure
- Installed agents no longer depend on a separate Forge runtime on PATH
- Install state is auditable through Forge-owned metadata
  </success_criteria>

<output>
After completion, create `.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-02-SUMMARY.md`
</output>
