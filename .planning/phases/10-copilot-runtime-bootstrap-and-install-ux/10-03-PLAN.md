---
phase: 10-copilot-runtime-bootstrap-and-install-ux
plan: "03"
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - README.md
  - docs/releasing.md
  - tests/smoke/cli.test.ts
  - .planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-UAT.md
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Users can understand that Forge installs globally under `~/.copilot` on a fresh system."
    - "The support model explains how to verify a Copilot install on a target machine."
    - "Fresh-machine install verification is scripted and repeatable."
  artifacts:
    - path: README.md
      provides: user-facing Copilot install guidance with global runtime destination clarity
      min_lines: 30
    - path: .planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-UAT.md
      provides: fresh-machine install verification script
      min_lines: 20
    - path: tests/smoke/cli.test.ts
      provides: end-to-end first-run install verification
      min_lines: 80
  key_links:
    - from: README.md
      to: src/commands/install-assistants.ts
      via: documented install behavior
      pattern: Copilot
---

<objective>
Document and verify the fresh-machine global Copilot install model.

Purpose: Installer bootstrap work is incomplete if users and maintainers cannot verify where Forge installed, what runtime payload it bundled, and whether Copilot can now discover the agents.
Output: Updated install documentation plus repeatable UAT for fresh target systems.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-CONTEXT.md
@.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs for global ~/.copilot installs</name>
  <files>README.md</files>
  <action>Document the fresh-machine Copilot install flow, including the fixed global destination under `~/.copilot`, what Forge bootstraps automatically, what bundled tools it installs there, and how the user verifies that `/agent` can now discover the installed summonables.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The repository explains how Forge behaves on a clean target machine and where it installs Copilot assets and bundled tools.</done>
</task>

<task type="auto">
  <name>Task 2: Add UAT and support verification for first-run installs</name>
  <files>.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-UAT.md, tests/smoke/cli.test.ts, docs/releasing.md</files>
  <action>Add a first-run install UAT script that uses an empty home target, verifies runtime bootstrap, confirms the expected Copilot asset files and bundled tools exist under `~/.copilot` after install, and documents any remaining support boundaries or manual verification steps maintainers should know.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Forge has a repeatable verification script for fresh-machine Copilot installs.</done>
</task>

</tasks>

<verification>
- [ ] `README.md` explains fresh-machine global Copilot installs clearly
- [ ] Phase 10 has a UAT script for empty `~/.copilot` installs
- [ ] `npm test` verifies end-to-end fresh-machine global install behavior
- [ ] Support boundaries and verification steps are documented
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Users can understand and verify first-run Copilot installs
- Users can verify the bundled runtime exists under `~/.copilot`
- Maintainers have a clear support story for missing-runtime target systems
  </success_criteria>

<output>
After completion, create `.planning/phases/10-copilot-runtime-bootstrap-and-install-ux/10-03-SUMMARY.md`
</output>
