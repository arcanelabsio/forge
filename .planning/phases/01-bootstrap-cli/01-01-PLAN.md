---
phase: 01-bootstrap-cli
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/cli.ts
  - src/program.ts
autonomous: true
requirements:
  - INVK-01
  - DELV-01
user_setup: []
must_haves:
  truths:
    - "The repository root is the npm package boundary for Forge."
    - "The published package exposes a single forge-ai-assist CLI binary suitable for npx invocation."
    - "A local build produces a runnable dist/cli.js entrypoint without depending on the nested Python scaffold."
  artifacts:
    - path: package.json
      provides: npm package metadata, scripts, and bin mapping
      contains: "\"forge-ai-assist\""
    - path: tsconfig.json
      provides: TypeScript build configuration for the CLI runtime
      contains: "\"outDir\""
    - path: src/cli.ts
      provides: shebang CLI entrypoint
      min_lines: 8
    - path: src/program.ts
      provides: root command/program factory
      min_lines: 20
  key_links:
    - from: package.json
      to: dist/cli.js
      via: npm bin mapping
      pattern: "forge-ai-assist"
    - from: src/cli.ts
      to: src/program.ts
      via: async program bootstrap
      pattern: "program"
---

<objective>
Establish the root npm package and CLI runtime boundary for Forge.

Purpose: Phase 1 depends on a real Node-based package entrypoint instead of the existing nested Python scaffold.
Output: Root package metadata, TypeScript build config, and a minimal CLI/program entrypoint that later plans can extend.
</objective>

<execution_context>
@/Users/ajit.gunturi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajit.gunturi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-cli/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root npm package contract</name>
  <files>package.json, tsconfig.json</files>
  <action>Create a root-level Node 22 + TypeScript package named `forge-ai-assist` with ESM settings, a single `bin` entry for `forge-ai-assist`, a `files` allowlist for published output, and scripts for build and smoke invocation. Keep the nested `forge/` Python project untouched; this plan establishes the repo root as the product package boundary instead of trying to repurpose the brownfield scaffold.</action>
  <verify>
    <automated>test -f package.json &amp;&amp; test -f tsconfig.json &amp;&amp; rg '"bin"|forge-ai-assist|files' package.json</automated>
  </verify>
  <done>The repository root declares one publishable npm package with a single CLI binary and a TypeScript build target.</done>
</task>

<task type="auto">
  <name>Task 2: Build the minimal CLI bootstrap</name>
  <files>src/cli.ts, src/program.ts</files>
  <action>Create a shebang `src/cli.ts` entrypoint and a `src/program.ts` builder using `commander`. The program should expose the package name, help, and version output plus a top-level async execution path that later plans can extend with repository-aware commands. Keep side effects out of this layer; this file pair should only parse CLI input, run the program, and surface typed user-facing errors cleanly.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A build produces `dist/cli.js`, and the CLI can start from the root package entrypoint without touching repository files.</done>
</task>

<task type="auto">
  <name>Task 3: Verify publishable npx packaging semantics locally</name>
  <files>package.json</files>
  <action>Confirm the package metadata is actually viable for the required `npx forge-ai-assist@latest` path. First, check npm registry name availability/ownership for `forge-ai-assist`; if the unscoped name is unavailable and not already controlled for this project, stop and surface that requirement conflict instead of silently choosing a different package name. Then validate the `bin` layout and publish contents with a local tarball flow that installs or executes the packed artifact the same way `npx` would consume a published package, ensuring the tarball exposes one runnable `forge-ai-assist` binary and does not include the brownfield repository contents.</action>
  <verify>
    <automated>npm view forge-ai-assist name version --json &amp;&amp; PACK_FILE=$(npm pack --json --silent | node -e "const chunks=[];process.stdin.on('data',(d)=>chunks.push(d));process.stdin.on('end',()=>{const parsed=JSON.parse(Buffer.concat(chunks).toString());process.stdout.write(parsed[0].filename);});") &amp;&amp; TMPDIR=$(mktemp -d) &amp;&amp; npm install --prefix "$TMPDIR" "./$PACK_FILE" &amp;&amp; "$TMPDIR/node_modules/.bin/forge-ai-assist" --help</automated>
  </verify>
  <done>Forge has either validated the exact npm package name needed for `npx forge-ai-assist@latest` or surfaced a hard blocker, and the locally packed artifact installs with one unambiguous runnable CLI binary.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds from the repository root
- [ ] npm registry validation does not leave the required `forge-ai-assist` package name ambiguous
- [ ] A packed tarball installs into a temp directory and runs `forge-ai-assist --help`
- [ ] `node dist/cli.js --help` prints Forge CLI help without runtime errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge has a root npm package boundary with one CLI executable mapped for npm/npx delivery and an explicit validation path for the required published invocation
  </success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-cli/01-01-SUMMARY.md`
</output>
