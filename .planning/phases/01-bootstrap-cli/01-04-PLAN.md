---
phase: 01-bootstrap-cli
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - src/program.ts
  - src/commands/bootstrap.ts
  - src/cli.ts
autonomous: true
requirements:
  - INVK-01
  - INVK-04
  - SIDE-01
  - SIDE-02
  - SIDE-03
  - SIDE-04
user_setup: []
must_haves:
  truths:
    - "Running the repository bootstrap flow from inside a Git repository initializes or reuses the `.forge` sidecar."
    - "Running the same repository flow outside Git exits cleanly with the explicit repository-required message."
    - "The bootstrap flow performs no writes outside the `.forge` sidecar."
  artifacts:
    - path: src/commands/bootstrap.ts
      provides: repository-aware bootstrap command handler
      min_lines: 35
    - path: src/program.ts
      provides: command registration for the bootstrap flow
      min_lines: 25
    - path: src/cli.ts
      provides: top-level error mapping and exit behavior
      min_lines: 10
  key_links:
    - from: src/program.ts
      to: src/commands/bootstrap.ts
      via: command registration
      pattern: "bootstrap"
    - from: src/commands/bootstrap.ts
      to: src/services/git.ts
      via: repo root guard
      pattern: "resolve"
    - from: src/commands/bootstrap.ts
      to: src/services/sidecar.ts
      via: sidecar initialization
      pattern: "sidecar"
---

<objective>
Wire the first real repository bootstrap command through the CLI using the Git guard and sidecar services.

Purpose: This is the Phase 1 user-facing proof that Forge can launch from npm, validate repository context, and initialize sidecar state without mutating the host repository.
Output: A bootstrap command connected to the CLI entrypoint with guarded execution and sidecar-only writes.
</objective>

<execution_context>
@/Users/ajit.gunturi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajit.gunturi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-cli/01-RESEARCH.md
@.planning/phases/01-bootstrap-cli/01-01-SUMMARY.md
@.planning/phases/01-bootstrap-cli/01-02-SUMMARY.md
@.planning/phases/01-bootstrap-cli/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the repository bootstrap command handler</name>
  <files>src/commands/bootstrap.ts</files>
  <action>Create the first repository-bound command for Forge. It should resolve the Git root through the Git service, initialize or reuse the `.forge` sidecar through the sidecar service, and print a concise success summary describing the repo root and sidecar path. Do not add analysis or planning logic yet; this command is only the Phase 1 bootstrap proof.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The bootstrap command can execute the phase-1 repository setup path using shared services and sidecar-only writes.</done>
</task>

<task type="auto">
  <name>Task 2: Register the command and map user-facing errors</name>
  <files>src/program.ts, src/cli.ts</files>
  <action>Register the bootstrap command in the root program and make the CLI convert typed guardrail failures into clean stderr output plus a non-zero exit code. Preserve help/version behavior from Plan 01, and ensure out-of-repo execution prints the explicit repository-required message rather than a raw exception.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js --help</automated>
  </verify>
  <done>The CLI exposes the repository bootstrap command, keeps standard help output, and surfaces clean user-facing guardrail failures.</done>
</task>

<task type="auto">
  <name>Task 3: Prove sidecar-only bootstrap behavior end to end</name>
  <files>src/commands/bootstrap.ts, src/cli.ts</files>
  <action>Run the command once in this Git repository and once from a temporary non-repository directory to confirm both happy-path and guardrail behavior. Capture any needed command-output shaping in the CLI layer, but do not add writes outside `.forge` or mutate repository files like `.gitignore`.</action>
  <verify>
    <automated>npm run build &amp;&amp; node dist/cli.js bootstrap &amp;&amp; TMPDIR=$(mktemp -d) &amp;&amp; STDERR_FILE="$TMPDIR/bootstrap.stderr" &amp;&amp; if (cd "$TMPDIR" &amp;&amp; node /Users/ajit.gunturi/workspaces/playground/forge/dist/cli.js bootstrap 1&gt;/dev/null 2&gt;"$STDERR_FILE"); then echo "expected non-zero exit outside git repo" &amp;&amp; exit 1; else test -s "$STDERR_FILE" &amp;&amp; rg "Git repository|repository" "$STDERR_FILE" &amp;&amp; test ! -e "$TMPDIR/.forge"; fi</automated>
  </verify>
  <done>Forge initializes `.forge` when run inside a repository, fails cleanly outside Git, and does not create or modify non-sidecar repository files.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds after command wiring
- [ ] `node dist/cli.js bootstrap` initializes or reuses `.forge` in this repository
- [ ] Running the same command from a non-Git directory prints the explicit repository-required message and does not create sidecar state there
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge now has a working repository bootstrap command that respects the sidecar-only v1 boundary
  </success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-cli/01-04-SUMMARY.md`
</output>
