---
phase: 01-bootstrap-cli
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/config/sidecar.ts
  - src/services/metadata.ts
  - src/services/sidecar.ts
autonomous: true
requirements:
  - SIDE-01
  - SIDE-03
  - SIDE-04
user_setup: []
must_haves:
  truths:
    - "Forge uses exactly one canonical sidecar directory per repository."
    - "Re-running bootstrap in the same repository reuses prior sidecar state instead of duplicating artifact trees."
    - "Sidecar metadata records enough run information to understand what Forge already created."
  artifacts:
    - path: src/config/sidecar.ts
      provides: canonical sidecar path contract
      contains: ".forge"
    - path: src/services/metadata.ts
      provides: metadata schema and atomic read/write helpers
      min_lines: 25
    - path: src/services/sidecar.ts
      provides: sidecar creation and reuse lifecycle
      min_lines: 35
  key_links:
    - from: src/services/sidecar.ts
      to: src/config/sidecar.ts
      via: shared sidecar directory constant
      pattern: "SIDECAR"
    - from: src/services/sidecar.ts
      to: src/services/metadata.ts
      via: metadata initialization and persistence
      pattern: "metadata"
---

<objective>
Define the canonical Forge sidecar and its idempotent metadata lifecycle.

Purpose: Later analysis and planning phases need one stable repository-owned workspace that can be reused safely across reruns.
Output: A `.forge` sidecar contract, metadata schema, and sidecar manager with atomic persistence behavior.
</objective>

<execution_context>
@/Users/ajit.gunturi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajit.gunturi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-cli/01-RESEARCH.md
@.planning/phases/01-bootstrap-cli/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the sidecar contract to one canonical directory</name>
  <files>src/config/sidecar.ts, src/services/sidecar.ts</files>
  <action>Choose `.forge` as the single repo-local sidecar directory and encode that choice in a shared config module instead of scattering string literals. The sidecar manager should derive paths from the resolved repo root and refuse to create alternate per-command roots so later phases inherit one stable contract.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has one source of truth for the sidecar directory name and path derivation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement atomic sidecar metadata helpers</name>
  <files>src/services/metadata.ts</files>
  <action>Create metadata schemas and helpers for reading, initializing, and atomically replacing sidecar metadata JSON. Include a schema version, timestamps, and enough run history or manifest information to understand prior bootstrap output. Use temp-file plus rename semantics instead of in-place overwrites so reruns do not corrupt metadata.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Metadata writes are schema-validated, atomic, and capable of representing prior Forge runs in a reusable way.</done>
</task>

<task type="auto">
  <name>Task 3: Add idempotent sidecar initialization behavior</name>
  <files>src/services/sidecar.ts</files>
  <action>Implement a sidecar lifecycle entrypoint that creates `.forge` on first run, reuses it on later runs, and updates metadata without multiplying top-level artifact trees. Keep this service strictly scoped to the sidecar directory so it never writes into repository source paths outside `.forge`.</action>
  <verify>
    <automated>npm run build &amp;&amp; TMPDIR=$(mktemp -d) &amp;&amp; git -C "$TMPDIR" init &amp;&amp; node -e "import('./dist/services/sidecar.js').then(async ({initializeSidecar}) =&gt; { const repo=process.argv[1]; const first=await initializeSidecar(repo); const second=await initializeSidecar(repo); if (first.sidecarPath !== second.sidecarPath) throw new Error('sidecar path changed across rerun'); if (first.metadataPath !== second.metadataPath) throw new Error('metadata path changed across rerun'); }).catch((error) =&gt; { console.error(error); process.exit(1); });" "$TMPDIR" &amp;&amp; test -d "$TMPDIR/.forge" &amp;&amp; test -f "$TMPDIR/.forge/metadata.json" &amp;&amp; test "$(find "$TMPDIR" -maxdepth 1 -type d -name '.forge' | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <done>Repeated initialization reuses the same sidecar root and metadata file without duplicate directories or corrupted state.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with sidecar and metadata services
- [ ] `node -e "import('./dist/services/sidecar.js')"` loads after build
- [ ] Running sidecar initialization twice against the same temp repo keeps one stable `.forge` root and one reused metadata file
- [ ] The sidecar contract and metadata helpers only target the `.forge` tree
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge owns exactly one reusable `.forge` sidecar with metadata designed for safe reruns and verified reuse across repeated bootstrap calls
  </success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-cli/01-03-SUMMARY.md`
</output>
