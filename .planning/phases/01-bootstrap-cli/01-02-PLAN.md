---
phase: 01-bootstrap-cli
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/services/git.ts
  - src/lib/errors.ts
autonomous: true
requirements:
  - INVK-04
user_setup: []
must_haves:
  truths:
    - "Repository-scoped Forge flows fail fast outside a Git repository."
    - "The user sees an explicit, stable error message instead of a stack trace when a repo command is launched outside Git."
    - "Repository root resolution works from nested directories by using Git itself rather than path heuristics."
  artifacts:
    - path: src/services/git.ts
      provides: git repository detection and root resolution helpers
      min_lines: 25
      contains: rev-parse
    - path: src/lib/errors.ts
      provides: typed user-facing CLI errors for guard failures
      min_lines: 15
  key_links:
    - from: src/services/git.ts
      to: git
      via: execa subprocess for rev-parse
      pattern: "rev-parse"
---

<objective>
Add Git-native repository detection and the explicit out-of-repo failure path required for repository-bound commands.

Purpose: Every later Forge workflow depends on accurate repository context and a clean exit when no Git repository is available.
Output: A reusable Git service and typed CLI error helpers for repository guardrails.
</objective>

<execution_context>
@/Users/ajit.gunturi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajit.gunturi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-cli/01-RESEARCH.md
@.planning/phases/01-bootstrap-cli/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Git-native repository resolution helpers</name>
  <files>src/services/git.ts</files>
  <action>Create a dedicated Git service that uses `execa` to run `git rev-parse --is-inside-work-tree` and `git rev-parse --show-toplevel`. Return the normalized repository root for valid repos, and do not replace this with manual `.git` path scanning because worktrees and nested invocation must remain correct.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge has a reusable Git service that can confirm repo context and resolve the canonical root from any nested working directory.</done>
</task>

<task type="auto">
  <name>Task 2: Define explicit CLI guardrail errors</name>
  <files>src/lib/errors.ts</files>
  <action>Add typed error primitives for user-facing CLI failures, including a repository-required error that carries the exact message Forge should print when a repository-analysis flow is invoked outside Git. Keep the message concise and stable because later command wiring will surface it directly to users.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Repository guard failures can be surfaced through typed errors rather than raw subprocess exceptions or stack traces.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the new Git service and error types
- [ ] `node -e "import('./dist/services/git.js')"` loads without module errors after build
- [ ] The repository-required error text is present and ready for command wiring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Forge has a reliable Git-based guard that can stop repository flows cleanly outside a repository
  </success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-cli/01-02-SUMMARY.md`
</output>
