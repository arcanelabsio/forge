---
phase: 07-installer-simplification-and-github-discussions-ingest
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/services/assistants/forge-agent.ts
  - src/services/git.ts
  - src/services/analysis/artifacts.ts
  - src/services
  - tests
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "The discussions workflow checks for `GH_TOKEN` or `GITHUB_TOKEN` before attempting GitHub API calls."
    - "When both token variables are missing, Forge returns a clear shell-oriented instruction telling the user to set one."
    - "The discussions workflow supports user-provided filters for relative dates, explicit bounds, and category selection."
  artifacts:
    - path: src/services
      provides: auth preflight and filter normalization for GitHub Discussions queries
      min_lines: 1
    - path: src/services/assistants/forge-agent.ts
      provides: summonable instructions that explain token requirements and accepted filter forms
      min_lines: 20
    - path: src/services/analysis/artifacts.ts
      provides: persisted metadata for normalized discussion filters and fetch bounds
      min_lines: 60
    - path: tests
      provides: coverage for missing-token, relative-date, explicit-bound, and category-filter behavior
      min_lines: 1
  key_links:
    - from: src/services/assistants/forge-agent.ts
      to: src/services
      via: user-provided fetch filters and token requirements passed into the discussions workflow
      pattern: token
    - from: src/services
      to: src/services/analysis/artifacts.ts
      via: normalized filters persisted with discussion fetch runs
      pattern: filter
---

<objective>
Add auth preflight and user-driven filter support to the GitHub Discussions workflow.

Purpose: This plan turns the base discussions ingest capability into a usable assistant flow by requiring a GitHub token up front and allowing discussion fetches to be scoped by the time window and category the user asks for.
Output: Deterministic token checks, normalized date/category filters, and persisted fetch metadata that explains exactly what discussion slice was retrieved.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-CONTEXT.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-RESEARCH.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub token preflight with actionable user messaging</name>
  <files>src/services, src/services/assistants/forge-agent.ts</files>
  <action>Implement a preflight check that resolves a GitHub token from `GH_TOKEN` or `GITHUB_TOKEN` before any discussions fetch begins. If neither is present, stop immediately and return a clear message telling the user to export one of those variables in their shell before retrying. Update the summonable instructions so assistants know this precondition and can relay it consistently.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge never attempts a discussions fetch without first resolving a supported token or surfacing an actionable shell setup message.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize relative-date, explicit-bound, and category filters</name>
  <files>src/services, src/services/analysis/artifacts.ts</files>
  <action>Introduce a structured filter parser for discussions queries that supports natural shortcuts such as `today`, `yesterday`, and `last week`, explicit bounds such as `after <date>` and `before <date>`, and category constraints. Normalize all accepted filter forms into a deterministic internal object with concrete date bounds and category identifiers before the GitHub query runs, and persist that normalized filter object alongside each fetch run.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can turn assistant-provided discussion filters into a stable, auditable query shape before fetching data.</done>
</task>

<task type="auto">
  <name>Task 3: Add coverage for token failures and filter behavior</name>
  <files>tests</files>
  <action>Add automated tests that cover missing-token failure paths, token precedence between `GH_TOKEN` and `GITHUB_TOKEN`, relative date windows, explicit `after` and `before` bounds, invalid date input, and category filtering. Ensure the tests also prove that persisted discussion artifacts record the normalized filter metadata so later analysis can explain what was fetched.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The discussions workflow has regression coverage for auth preflight and filter normalization.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with auth preflight and filter normalization wired in
- [ ] Missing `GH_TOKEN` and `GITHUB_TOKEN` stops the workflow with an actionable shell setup message
- [ ] Relative filters such as `today`, `yesterday`, and `last week` normalize into explicit bounds deterministically
- [ ] Explicit `after` and `before` bounds plus category filters are supported and persisted in fetch metadata
- [ ] `npm test` covers token resolution and discussion filter behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No writes occur outside Forge-managed assistant targets and the `.forge` sidecar
- Forge can safely guide users through token setup and fetch a filtered slice of GitHub Discussions for later analysis
  </success_criteria>

<output>
After completion, create `.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-03-SUMMARY.md`
</output>
