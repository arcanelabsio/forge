---
phase: 07-installer-simplification-and-github-discussions-ingest
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/cli.ts
  - src/program.ts
  - src/commands/install-assistants.ts
  - src/services/assistants/install.ts
  - tests/smoke/cli.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "The npm package remains `forge-ai-assist` while the published executable is `forge`."
    - "Running `npx forge-ai-assist@latest` enters an interactive assistant-selection install flow by default."
    - "The legacy public command surface is removed from the product UX."
  artifacts:
    - path: package.json
      provides: published binary rename from `forge-ai-assist` to `forge`
      min_lines: 10
      contains: forge
    - path: src/program.ts
      provides: single-entry interactive installer flow instead of the legacy command registry
      min_lines: 35
    - path: src/services/assistants/install.ts
      provides: installer orchestration that can target user-selected assistants
      min_lines: 45
    - path: tests/smoke/cli.test.ts
      provides: coverage for the default installer flow and removed legacy commands
      min_lines: 40
  key_links:
    - from: src/cli.ts
      to: src/program.ts
      via: default executable entry
      pattern: createProgram
    - from: src/program.ts
      to: src/services/assistants/install.ts
      via: interactive assistant selection and install dispatch
      pattern: install
---

<objective>
Replace Forge's legacy multi-command CLI with the requested default interactive installer experience.

Purpose: This plan changes the public invocation model so `npx forge-ai-assist@latest` behaves like a single-purpose assistant installer and presents itself as `forge`.
Output: A renamed published binary, a default interactive assistant-selection flow, and validation that the old command-centric UX is no longer the primary surface.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-CONTEXT.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the published executable and remove the legacy command registry</name>
  <files>package.json, src/program.ts, src/cli.ts</files>
  <action>Change the package `bin` so npm publishes the executable as `forge` while keeping the package name as `forge-ai-assist`. Rework the CLI bootstrap so the default code path runs the installer flow instead of registering `bootstrap`, `analyze`, `plan`, `install-assistants`, and `install-copilot` as user-facing commands. If Commander remains, use it only as a thin wrapper around the single default action rather than as a command catalog.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The packaged binary is `forge`, and the public CLI no longer centers on explicit subcommands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the interactive assistant-selection installer flow</name>
  <files>src/program.ts, src/commands/install-assistants.ts, src/services/assistants/install.ts</files>
  <action>Add an interactive prompt that appears immediately on default execution and lets the user choose one or more assistant targets to install. Route the selected assistants through the existing assistant installation service, refactoring that service as needed so it can install a chosen subset cleanly and report installed, updated, skipped, and no-op outcomes in a concise summary.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Running the default entrypoint launches an assistant picker and completes install/update reporting without requiring a separate command verb.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the new installer UX and the removal of the old one</name>
  <files>tests/smoke/cli.test.ts, package.json</files>
  <action>Update smoke coverage so it verifies the packed npm artifact still executes through `npx forge-ai-assist@latest` semantics, the binary name is `forge`, and the default runtime path launches the installer flow. Add assertions that the old command help surface is no longer exposed as the expected product experience. Keep validation sidecar-safe and non-destructive.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The repo has regression coverage for the renamed binary and the new single-entry installer behavior.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds after the CLI entrypoint refactor
- [ ] The published package maps its executable to `forge` while keeping the package name `forge-ai-assist`
- [ ] Default execution launches the interactive assistant-selection install flow
- [ ] Legacy subcommands are no longer the primary documented runtime surface
- [ ] `npm test` covers the new install-first UX
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No writes occur outside Forge-managed assistant targets and the `.forge` sidecar
- Forge now behaves like a single-entry interactive installer rather than a multi-command CLI
  </success_criteria>

<output>
After completion, create `.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-01-SUMMARY.md`
</output>
