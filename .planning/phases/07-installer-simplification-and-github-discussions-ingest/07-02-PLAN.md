---
phase: 07-installer-simplification-and-github-discussions-ingest
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/contracts/summonable-entry.ts
  - src/services/assistants/forge-agent.ts
  - src/services/git.ts
  - src/config/analysis.ts
  - src/services/analysis/artifacts.ts
  - src/services/assistants/install.ts
  - tests
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Forge installs a summonable GitHub Discussions fetch capability for supported assistants."
    - "The fetch flow derives the GitHub repository identity from the current repository remote."
    - "Fetched discussions are stored as durable sidecar artifacts for later analysis."
  artifacts:
    - path: src/services/git.ts
      provides: repository remote parsing and GitHub owner/repo derivation
      min_lines: 70
      contains: remote
    - path: src/services/analysis/artifacts.ts
      provides: sidecar persistence model that can be extended or mirrored for discussions runs
      min_lines: 60
    - path: src/services/assistants/forge-agent.ts
      provides: summonable entry for the GitHub Discussions fetch capability
      min_lines: 20
    - path: tests
      provides: coverage for remote parsing, fetch persistence, and sidecar-only behavior
      min_lines: 1
  key_links:
    - from: src/services/git.ts
      to: src/services/assistants/install.ts
      via: repo identity passed into installed or invoked fetch workflow
      pattern: remote
    - from: src/services/assistants/forge-agent.ts
      to: src/services/assistants/install.ts
      via: summonable entry installation
      pattern: forge-agent
---

<objective>
Add a summonable GitHub Discussions fetch capability that writes durable repository discussion artifacts into the Forge sidecar.

Purpose: This plan gives assistants a concrete Forge-managed workflow for acquiring GitHub Discussions from the current repository, without yet solving the later analysis prompt layer.
Output: A new assistant-facing fetcher entry, repository-remote parsing, authenticated GitHub discussion retrieval, and sidecar persistence for raw discussion data.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-CONTEXT.md
@.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-RESEARCH.md
@src/services/git.ts
@src/services/analysis/artifacts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repository-remote parsing and discussions sidecar artifact paths</name>
  <files>src/services/git.ts, src/config/analysis.ts, src/services/analysis/artifacts.ts</files>
  <action>Extend the Git service so Forge can derive a normalized GitHub `owner/repo` from the current repository's `origin` remote, covering both HTTPS and SSH remote formats with explicit failure modes for unsupported remotes. Add a dedicated discussions artifact subtree under `.forge` with historical run storage and a latest pointer, either by extending the current artifact patterns or by creating a parallel persistence service that follows the same durability rules.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can resolve the current GitHub repository identity and has sidecar paths ready for durable discussion fetch artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the GitHub Discussions fetcher and summonable assistant entry</name>
  <files>src/services/assistants/forge-agent.ts, src/services/assistants/install.ts, src/contracts/summonable-entry.ts</files>
  <action>Add a new shared summonable entry that tells supported assistants how to invoke a Forge-managed GitHub Discussions fetch workflow. Implement the underlying fetch path to query GitHub Discussions for the derived `owner/repo`, handle authenticated GraphQL pagination, normalize raw results into machine-readable JSON, and persist each run into the `.forge` discussions subtree. Keep downstream summarization or recommendation logic out of scope for this plan.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Forge can install a summonable discussions-fetch capability and back it with real sidecar-persisted GitHub discussion data.</done>
</task>

<task type="auto">
  <name>Task 3: Cover error handling and sidecar-only persistence</name>
  <files>tests</files>
  <action>Add automated tests for remote parsing, unsupported remote handling, missing-auth behavior, and sidecar persistence. If a live-network smoke path is included, gate it behind explicit environment availability so normal test runs remain deterministic. The core automated coverage should prove that successful discussion fetches write only under `.forge` and that failed fetch attempts leave a clear, non-destructive error path.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The fetch capability has coverage for GitHub remote derivation, authentication failures, and sidecar-only persistence.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the GitHub Discussions fetch capability wired in
- [ ] Forge can derive `owner/repo` from the current repository `origin` remote
- [ ] Successful fetches persist raw discussion artifacts and latest pointers under `.forge`
- [ ] Missing auth, disabled discussions, or unsupported remotes fail clearly without destructive side effects
- [ ] `npm test` covers the fetch flow and sidecar persistence rules
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No writes occur outside Forge-managed assistant targets and the `.forge` sidecar
- Forge can install and back a summonable GitHub Discussions fetch workflow that later phases can analyze
  </success_criteria>

<output>
After completion, create `.planning/phases/07-installer-simplification-and-github-discussions-ingest/07-02-SUMMARY.md`
</output>
