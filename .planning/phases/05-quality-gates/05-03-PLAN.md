---
phase: 05-quality-gates
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [tests/smoke/cli.test.ts, package.json]
autonomous: true
requirements: [DELV-04]

must_haves:
  truths:
    - "npm run test:smoke runs and passes against temporary Git repositories"
    - "Forge bootstrap creates .forge/ metadata in a real temp git repo"
    - "Forge analyze and plan flows work end-to-end without mocking in temp repos"
  artifacts:
    - path: "tests/smoke/cli.test.ts"
      provides: "Smoke tests against real Git repos"
  key_links:
    - from: "package.json"
      to: "tests/smoke/cli.test.ts"
      via: "scripts.test:smoke"
---

<objective>
Implement smoke tests that validate the CLI workflows against real, temporary Git repositories.
Purpose: Ensure the end-to-end integration of all components (Git, Sidecar, Analysis, Planning) works correctly in a realistic environment.
Output: Working smoke test suite that uses temporary directories and real git commands.
</objective>

<execution_context>
@/Users/ajitg/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quality-gates/05-RESEARCH.md
@src/cli.ts
@src/program.ts
@package.json

<interfaces>
The smoke test will invoke the CLI via subprocess:
```bash
node dist/cli.js bootstrap
node dist/cli.js analyze
node dist/cli.js plan --task "test task"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Smoke Test Infrastructure</name>
  <files>tests/smoke/cli.test.ts, package.json</files>
  <action>Create a smoke test suite using Vitest that utilizes temporary directories (e.g., via `os.tmpdir()` or `vitest` utilities if available) and `execa` to run real `git init` and `node dist/cli.js` commands. Add "test:smoke" script to package.json. Ensure the tests clean up temporary directories after completion.</action>
  <verify>npm run test:smoke -t "infrastructure"</verify>
  <done>Smoke test infrastructure is ready to run CLI commands against real git repositories.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI Flow Smoke Tests</name>
  <files>tests/smoke/cli.test.ts</files>
  <action>Implement smoke tests for the core CLI flows:
1. `bootstrap`: Verify it creates `.forge/metadata.json` in a new repo and exits with error outside a repo.
2. `analyze`: Verify it generates analysis artifacts in `.forge/analysis/`.
3. `plan`: Verify it generates a plan in `.forge/plans/` based on a previous analysis.
4. `idempotency`: Verify multiple runs of bootstrap/analyze/plan do not corrupt the sidecar or metadata.</action>
  <verify>npm run test:smoke</verify>
  <done>Full CLI workflows are verified by smoke tests against temporary Git repositories.</done>
</task>

</tasks>

<verification>
Run all tests: npm test && npm run test:smoke. Ensure all tests pass.
</verification>

<success_criteria>
- Smoke tests pass for bootstrap, analyze, and plan commands.
- CLI correctly handles repository guardrails in a real filesystem.
- Sidecar artifacts are created and persisted correctly across multiple runs in a real repository context.
</success_criteria>

<output>
After completion, create .planning/phases/05-quality-gates/05-03-SUMMARY.md
</output>
