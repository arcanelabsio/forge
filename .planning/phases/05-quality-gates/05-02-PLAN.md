---
phase: 05-quality-gates
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [tests/unit/analysis/run.test.ts, tests/unit/planning/generator.test.ts, tests/unit/planning/run.test.ts]
autonomous: true
requirements: [DELV-03]

must_haves:
  truths:
    - "npm test runs and passes for analysis and planning unit tests"
    - "Repository analysis logic (stack detection, structure) is tested with mock data"
    - "Action-plan generation logic is tested with mock analysis results"
  artifacts:
    - path: "tests/unit/analysis/run.test.ts"
      provides: "Analysis logic tests"
    - path: "tests/unit/planning/generator.test.ts"
      provides: "Plan generation logic tests"
    - path: "tests/unit/planning/run.test.ts"
      provides: "Planning workflow tests"
  key_links:
    - from: "tests/unit/planning/run.test.ts"
      to: "src/services/analysis/run.ts"
      via: "mocked import"
---

<objective>
Implement unit tests for the Repository Analysis and Planning logic.
Purpose: Ensure that the "Sidecar-First" analysis engine and action-planning flows are reliable and correctly handle various repository facts.
Output: Working unit tests for analysis and planning modules.
</objective>

<execution_context>
@/Users/ajitg/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-quality-gates/05-RESEARCH.md
@src/services/analysis/run.ts
@src/services/analysis/stack.ts
@src/services/analysis/structure.ts
@src/services/planning/run.ts
@src/services/planning/generator.ts

<interfaces>
From src/services/analysis/run.ts:
```typescript
export async function runAnalysis(repoRoot: string): Promise<AnalysisRun>;
```

From src/services/planning/run.ts:
```typescript
export async function runPlanningFlow(
  context: SidecarContext,
  task: string,
  options?: PlanningOptions
): Promise<PlanRun>;
```

From src/services/planning/generator.ts:
```typescript
export function generateActionPlan(
  analysis: AnalysisRun,
  task: string
): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for Analysis services</name>
  <files>tests/unit/analysis/run.test.ts</files>
  <action>Implement unit tests for runAnalysis and its underlying stack/structure detection services. Mock the filesystem to provide repository facts (e.g., package.json, src/ folder structure). Verify that analysis output correctly captures expected facts and distinguishes from inferred recommendations.</action>
  <verify>npx vitest tests/unit/analysis/run.test.ts</verify>
  <done>Repository analysis logic has unit test coverage for stack and structure detection.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for Planning services</name>
  <files>tests/unit/planning/generator.test.ts, tests/unit/planning/run.test.ts</files>
  <action>Implement unit tests for planning generator and workflow. Test generator with various analysis inputs (mocked AnalysisRun) to ensure it produces valid Markdown action plans. Test runPlanningFlow by mocking the underlying analysis load and persistence. Ensure plans are correctly associated with the task/context.</action>
  <verify>npx vitest tests/unit/planning/generator.test.ts tests/unit/planning/run.test.ts</verify>
  <done>Planning logic and plan generation have unit test coverage.</done>
</task>

</tasks>

<verification>
Run the full test suite: npm test. Ensure all unit tests pass and coverage report covers analysis/planning.
</verification>

<success_criteria>
- Unit tests for Analysis and Planning pass.
- Logic for stack detection, structure analysis, and plan generation is verified with mocked inputs.
- Coverage report shows >80% coverage for analysis and planning services.
</success_criteria>

<output>
After completion, create .planning/phases/05-quality-gates/05-02-SUMMARY.md
</output>
